from src.mode.rules.round_robin import RoundRobin
from src.mode.rules.usage import Usage
from src.mode.rules.weight import Weight

RULES = {
  "round_robin": RoundRobin,
  "usage": Usage,
  "weight": Weight
}